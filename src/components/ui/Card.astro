---
interface Props {
  href?: string;
  class?: string;
}

const { href, class: className } = Astro.props;
const Tag = href ? 'a' : 'div';
---

<Tag
  href={href}
  class:list={[
    'block bg-white rounded-xl border border-sand-200 overflow-hidden transition-all duration-200',
    href && 'hover:border-stone hover:-translate-y-0.5',
    className,
  ]}
>
  <slot />
</Tag>
